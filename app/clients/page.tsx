import { PageShell } from '@/components/page-shell';

export default function ClientsPage() {
  return <PageShell title="Clients" description="Run enrichment jobs and maintain account-level context." />;
}
